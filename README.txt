Prerequisites - JDK 1.8 or higher

To compile and run the program go to the root folder of the project and open the terminal. Then use the commands below.

Compile
javac -d temp src/*.java

Run
java -cp temp Main
